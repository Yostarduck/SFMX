version: 1.0.{build}

# Only build pull requests, skip duplicate branch builds
skip_branch_with_pr: true

image:
  - Visual Studio 2022
  - Ubuntu

platform: x64

configuration:
  - Debug
  - Release

cache:
  - Build/_deps/sfml-src -> SketchGfx/CMakeLists.txt
  - Build/_deps/sfml-build -> SketchGfx/CMakeLists.txt

install:
  - cmake --version
  
# Webhook sent via on_finish build scripts (not the notifications provider)
# because Appveyor's .NET client rejects Content-Type as a request header,
# and Discord requires Content-Type: application/json.

#
# ── Windows ──────────────────────────────────────────────────────────
#
for:
-
  matrix:
    only:
      - image: Visual Studio 2022

  build_script:
    - cmake -B Build -A x64
    - cmake --build Build --config %CONFIGURATION%

  after_build:
    - ps: |
        $libDir = "Build\_deps\sfml-build\lib\$env:CONFIGURATION"
        $binDir = "Build\_deps\sfml-build\bin"
        $outDir = "Build\x64\$env:CONFIGURATION"
        if (Test-Path $libDir) { Copy-Item "$libDir\*.dll" $outDir }
        if (Test-Path $binDir) { Copy-Item "$binDir\*.dll" $outDir }

  test_script:
    - cd Build
    - ctest -C %CONFIGURATION% --output-on-failure --timeout 30 || ver
    - cd ..

  on_finish:
    - ps: |
        $url = "https://discord.com/api/webhooks/1516801263591886899/1VN5Bnk_v-O_VkwWjJJYCoV24Y1YLW01ssh-EPeZBDYj7buEHGCHQFijyXyWZFH6Dls2"
        $color = if ($env:APPVEYOR_BUILD_STATUS -eq "success") { 65280 } else { 16711680 }
        $commitMsg = "$env:APPVEYOR_REPO_COMMIT_MESSAGE".Split("`n")[0]
        $content = "[$env:APPVEYOR_PROJECT_NAME]($env:APPVEYOR_URL/project/$env:APPVEYOR_ACCOUNT_NAME/$env:APPVEYOR_PROJECT_SLUG/build/$env:APPVEYOR_BUILD_VERSION) build **$env:APPVEYOR_BUILD_STATUS** on **$env:APPVEYOR_REPO_BRANCH** ($env:APPVEYOR_REPO_COMMIT)"
        $body = @{ content = $content; embeds = @(@{ title = $commitMsg; color = $color }) } | ConvertTo-Json -Compress -Depth 3
        Invoke-RestMethod -Uri $url -Method Post -Body $body -ContentType "application/json"

  artifacts:
    - path: Build\x64\%CONFIGURATION%\Game.exe
      name: Game-%CONFIGURATION%
    - path: Build\x64\%CONFIGURATION%\*.dll
      name: RuntimeDLLs-%CONFIGURATION%

#
# ── Linux ────────────────────────────────────────────────────────────
#
-
  matrix:
    only:
      - image: Ubuntu

  before_build:
    - sh: sudo apt-get update -qq && sudo apt-get install -y -qq libudev-dev libvorbis-dev libogg-dev libflac-dev libmbedtls-dev libssh2-1-dev

  build_script:
    - cmake -B Build
    - cmake --build Build --config $CONFIGURATION

  after_build:
    - sh: |
        LIBDIR="Build/_deps/sfml-build/lib"
        OUTDIR="Build/x64/$CONFIGURATION"
        if [ -d "$LIBDIR" ]; then
          mkdir -p "$OUTDIR"
          cp "$LIBDIR"/libsfml-*.so* "$OUTDIR" 2>/dev/null || true
        fi

  test_script:
    - sh: |
        export LD_LIBRARY_PATH="Build/_deps/sfml-build/lib:Build/x64/$CONFIGURATION"
        cd Build && ctest -C $CONFIGURATION --output-on-failure --timeout 30 || true

  on_finish:
    - sh: |
        URL="https://discord.com/api/webhooks/1516801263591886899/1VN5Bnk_v-O_VkwWjJJYCoV24Y1YLW01ssh-EPeZBDYj7buEHGCHQFijyXyWZFH6Dls2"
        if [ "$APPVEYOR_BUILD_STATUS" = "success" ]; then COLOR=65280; else COLOR=16711680; fi
        COMMIT_MSG=$(echo "$APPVEYOR_REPO_COMMIT_MESSAGE" | head -1)
        CONTENT="[$APPVEYOR_PROJECT_NAME]($APPVEYOR_URL/project/$APPVEYOR_ACCOUNT_NAME/$APPVEYOR_PROJECT_SLUG/build/$APPVEYOR_BUILD_VERSION) build **$APPVEYOR_BUILD_STATUS** on **$APPVEYOR_REPO_BRANCH** ($APPVEYOR_REPO_COMMIT)"
        export URL COLOR COMMIT_MSG CONTENT
        python3 -c "import json,os;u=os.environ['URL'];c=int(os.environ['COLOR']);m=os.environ.get('COMMIT_MSG','');t=os.environ['CONTENT'];b=json.dumps({'content':t,'embeds':[{'title':m,'color':c}]});import urllib.request as r;r.urlopen(r.Request(u,data=b.encode(),headers={'Content-Type':'application/json'},method='POST'))" 2>/dev/null || true

  artifacts:
    - path: Build/x64/$(CONFIGURATION)/Game
      name: Game-$(CONFIGURATION)
